# MegaMind

- `design-brief.md` — implementation spec for the Electron app (tokens, type, layout, mark usage, build order).
- `icon/` — MegaMind mark assets. The brain half of the mark is not resolved yet — `megamind-logo-brainslot.svg` shows the placeholder slot where finished brain artwork will go. Everything else (circuit fan, M-in-circle) is final. Needs a designer pass before shipping on a commercial surface — see "Open items" in the design brief.
